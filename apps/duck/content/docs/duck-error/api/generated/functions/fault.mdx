---
title: "Function: fault()"
description: "The same declaration as detail, for a code an adapter can answer with itself."
---

## Call Signature

> **fault**(`status`: `number`): [`Fault`](../duck-error/namespaces/Brand/type-aliases/Fault)

Defined in: [brand.ts:74](https://github.com/gentleeduck/duck-error/blob/47cead89678ec7866f36fc8a3328634b06598fff/packages/duck-error/src/brand.ts#L74)

The same declaration as [detail](detail), for a code an adapter can answer with itself.

### Parameters

#### status

`number`

### Returns

[`Fault`](../duck-error/namespaces/Brand/type-aliases/Fault)

### Example

```ts
const REGISTRY = {
  INTERNAL: fault(500), // adapter-raisable, no meta
  STORAGE_FAILED: fault<{ cause?: string }>(500), // adapter-raisable, with meta
} as const satisfies Record<string, number>
```

## Call Signature

> **fault**\<`M` *extends* `object`\>(`status`: `number`): `number` & \{ `__carries`: `M`; \} & \{ `__fault`: `true`; \}

Defined in: [brand.ts:75](https://github.com/gentleeduck/duck-error/blob/47cead89678ec7866f36fc8a3328634b06598fff/packages/duck-error/src/brand.ts#L75)

The same declaration as [detail](detail), for a code an adapter can answer with itself.

### Type Parameters

#### M

`M` *extends* `object`

The meta shape the code carries, when given.

### Parameters

#### status

`number`

### Returns

`number` & \{ `__carries`: `M`; \} & \{ `__fault`: `true`; \}

### Example

```ts
const REGISTRY = {
  INTERNAL: fault(500), // adapter-raisable, no meta
  STORAGE_FAILED: fault<{ cause?: string }>(500), // adapter-raisable, with meta
} as const satisfies Record<string, number>
```

