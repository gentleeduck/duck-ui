---
title: "Type Alias: Bare<R *extends* Registry>"
description: "A code that needs nothing beyond itself."
---

> **Bare**\<`R` *extends* [`Registry`](Registry)\> = `{ [C in Code<R>]: [HasRequired<Meta<R, C>>] extends [never] ? C : never }`\[[`Code`](Code)\<`R`\>\]

Defined in: [kit.ts:40](https://github.com/gentleeduck/duck-error/blob/8232a7627a981ede4008575208050f91eebb8fb3/packages/duck-error/src/kit.ts#L40)

A code that needs nothing beyond itself.

## Type Parameters

### R

`R` *extends* [`Registry`](Registry)

## Example

```ts
type NoMetaNeeded = ErrorKit.Bare<typeof REGISTRY> // every code declared with a plain number, no detail()/fault()<M>
```

