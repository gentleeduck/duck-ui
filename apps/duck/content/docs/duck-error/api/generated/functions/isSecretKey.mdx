---
title: "Function: isSecretKey()"
description: "True when a key name looks like it might hold a secret — checked against a fixed pattern, not a per-registry allowlist."
---

> **isSecretKey**(`key`: `string`): `boolean`

Defined in: [scrub.ts:15](https://github.com/gentleeduck/duck-error/blob/8232a7627a981ede4008575208050f91eebb8fb3/packages/duck-error/src/scrub.ts#L15)

True when a key name looks like it might hold a secret — checked against a fixed pattern, not a per-registry allowlist.

## Parameters

### key

`string`

## Returns

`boolean`

## Example

```ts
isSecretKey('apiToken') // true — substring match, so it over-redacts rather than under-redacts
isSecretKey('widgetId') // false
```

